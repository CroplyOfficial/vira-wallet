import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tools.identity.vira',
  appName: 'vira',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
